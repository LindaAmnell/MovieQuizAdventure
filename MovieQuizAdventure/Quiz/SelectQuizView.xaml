<UserControl x:Class="MovieQuizAdventure.SelectQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin=" 40">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button x:Name="BackButton"
                Content="Back"
                Width="100" Height="40"
                Margin="40 0"
                Style="{StaticResource ButtonStyle}"
                FontWeight="Bold"
                FontFamily="ROG Fonts"
                HorizontalAlignment="Left"
                Click="BackButtonClick"/>
        
        <Grid Grid.Row="1"
              HorizontalAlignment="Center"
              MaxWidth="1200">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <TextBlock Grid.Row="0"
                       Text="QUIZ LIBRARY"
                       FontFamily="ROG Fonts"
                       FontSize="42"
                       FontWeight="Bold"
                       Foreground="#E7F6FF"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 25"/>


            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Available Quizzes"
                           FontFamily="ROG Fonts"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="#E7F6FF"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPlayMode}" Value="False">
                                    <Setter Property="Grid.ColumnSpan" Value="2"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Grid.Column="1"
                           Text="Quiz Categories"
                           FontFamily="ROG Fonts"
                           FontSize="22"
                           Margin="30 0 0 0"
                           FontWeight="Bold"
                           Foreground="#E7F6FF"
                           HorizontalAlignment="Center"
                           Visibility="{Binding IsPlayMode,
                                        Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
            <Grid Grid.Row="2">

                <Grid.ColumnDefinitions>

                    <ColumnDefinition>
                        <ColumnDefinition.Style>
                            <Style TargetType="ColumnDefinition">
                                <Setter Property="Width" Value="*"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlayMode}" Value="False">
                                        <Setter Property="Width" Value="Auto"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ColumnDefinition.Style>
                    </ColumnDefinition>

                    <ColumnDefinition Width="40"/>

                    <ColumnDefinition x:Name="CategoryColumn">
                        <ColumnDefinition.Style>
                            <Style TargetType="ColumnDefinition">
                                <Setter Property="Width" Value="*"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlayMode}" Value="False">
                                        <Setter Property="Width" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ColumnDefinition.Style>
                    </ColumnDefinition>

                </Grid.ColumnDefinitions>
                <ItemsControl x:Name="QuizList"
                              Grid.Column="0"
                              HorizontalAlignment="Center"
                              Background="Transparent"
                              BorderThickness="0">

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,5">

                                    <Button Content="{Binding Title}"
                                            Height="40" Width="200"
                                            Style="{StaticResource ButtonStyle}"
                                            Click="GoToChoosenQuiz"
                                            Tag="{Binding}"/>       


                                <StackPanel Orientation="Horizontal"
                                            Visibility="{Binding DataContext.IsEditMode,
                                            RelativeSource={RelativeSource AncestorType=UserControl},
                                            Converter={StaticResource BoolToVisibilityConverter}}">

                                    <Button Height="30" Width="30" Margin="20 10 0 10"
                                             Style="{StaticResource ButtonStyle}"
                                            Background="#42B4FF"
                                            Click="EditQuiz"
                                            Tag="{Binding}">
                                        <Image Source="/Images/pencil.png" Width="15"/>
                                    </Button>

                                    <Button Height="30" Width="30" Margin="15 10 0 10"
                                             Style="{StaticResource ButtonStyle}"
                                            Background="#42B4FF"
                                            Click="DeleteQuestionClick"
                                            Tag="{Binding}">
                                        <Image Source="/Images/trashcanicon.png" Width="15"/>
                                    </Button>

                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl Grid.Column="2"
                              ItemsSource="{Binding MovieCategories}"
                              Visibility="{Binding IsPlayMode,
                                           Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}"
                                    Margin="5"
                                    Padding="10"
                                    Style="{StaticResource ButtonStyle}"
                                    CommandParameter="{Binding}"
                                    Click="CategoryButtonClicked"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Grid>

        </Grid>
    </Grid>
</UserControl>
