<UserControl x:Class="MovieQuizAdventure.SelectQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieQuizAdventure"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="BackButton"   Content="Back"
               Width="100" Height="40" Margin="10"
               Style="{StaticResource ButtonStyle}"
               FontFamily="ROG Fonts"  HorizontalAlignment="Left"
               VerticalAlignment="Top" Click="BackButtonClick"/>
        
        <TextBlock Grid.Row="1"
                  Text="{Binding CurrentQuiz.Title}"
                   FontFamily="ROG Fonts"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#E7F6FF"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,20"/>

        <ScrollViewer Grid.Row="2"
              VerticalScrollBarVisibility="Hidden"
              HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="QuestionList" HorizontalAlignment="Center">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0 20" >
                    <TextBlock  Text="{Binding Statement}"
                               TextWrapping="Wrap"
                               Width="400"
                               Foreground="#E7F6FF"
                               FontFamily="ROG Fonts"
                               FontSize="16"
                               VerticalAlignment="Center"/>
                     

                            <Button Height="30" Width="30" Margin="5 10"
                                     Style="{StaticResource ButtonStyle}"
                                    Background="#42B4FF"
                                    Click="EditQuestionClick"
                                    Tag="{Binding}">    
                                <Image Source="/Images/pencil.png" Width="15"/>
                                    </Button>

                            <Button Height="30" Width="30" Margin="5 10"
                                     Style="{StaticResource ButtonStyle}"
                                    Background="#42B4FF"
                                    Click="DeleteQuestionClick"
                                    Tag="{Binding}">        
                                <Image Source="/Images/trashcanicon.png" Width="15"/>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
